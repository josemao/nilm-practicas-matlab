function rms=movingRMS(signal,W)

rms=[];
post_ant=1;
for i=W+1:W:length(signal)
    %signal chunk:
    chunk=signal(       );
    %square elements in chunk: 
    chunk=        ; 
    %evaluate mean in chunk: 
    chunk=        ;
    % root of mean: 
    chunk=       ;
    
    % Concatenate values
    rms=[      ];
    post_ant=i;
end
    
end